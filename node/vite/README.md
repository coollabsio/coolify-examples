# Vite

Vite static site example with vanilla TypeScript.

## Getting Started

```bash
npm install
npm run dev     # development
npm run build   # build to 'dist' directory
npm start       # serve static files
```

Vite is a build tool - for SSR, use a framework like Nuxt, SvelteKit, etc.
